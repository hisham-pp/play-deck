/**
 * Deterministic PRNG (mulberry32). Feeding a seed into the generator makes a
 * puzzle reproducible, which the tests rely on and a future "daily puzzle"
 * feature can reuse.
 */
export function createSeededRandom(seed: number): () => number {
  let state = seed >>> 0;
  return () => {
    state = (state + 0x6d2b79f5) >>> 0;
    let t = Math.imul(state ^ (state >>> 15), 1 | state);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}
